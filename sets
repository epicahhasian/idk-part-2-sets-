#Convering a list to a set
sample_list = [1,1,2,2,3,3]
sample_set = set(sample_list)
print (sample_set)
#sets are not indexable 
#print(sample_set[2])
#check if an elements exissts in a set 
if 4 in sample_set:
    print ("Yes")
else:
    print ("No")
if 3 in sample_set: 
    print ("Yes")
else:
    print ("No")
#adding elements to the set 
myset = set ([])
myset.add(3)
myset.add(3)
myset.add(2)
myset.add(1)
print(myset)
#remove elements from the set 
myset.remove(2)
print(myset)
#throws if elements is not present in the set 
#myset.remove(5)
#if not there then this code will just avoid the error 
myset.discard(5)
#set operations
#1) union 
#2) intersection
#3) different
#4) symmetric difference 

#union means addition of setss
a = {1,2,3,4,5}
b = {4,5,6,7,8}
print (a.union(b))
print (a|b)
#intersection means the common elements between two sets 
a = {1,2,3,4,5}
b = {4,5,6,7,8}
print (a.intersection(b))
print (a&b)
#difference of a and b is the elements that exist in A but not in b 
a = {1,2,3,4,5}
b = {4,5,6,7,8}
print (a.difference(b))
print (a-b)

